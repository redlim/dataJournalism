'use strict';

// Test specific configuration
// ===========================
module.exports = {
  // mariaDb connection options
  mysql : {
    conf:{
      host     : 'localhost',
      user     : 'root',
      password : 'bridget',
      database : 'anita'
    }
  }
};